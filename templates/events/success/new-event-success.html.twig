{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block title %}Réserver{% endblock %}

{% block body %}
    <section class="section">
        <div class="container">
            <h1 class="title is-3 has-text-primary">Évènement ajouté avec succès !</h1>
            <p class="notification is-success">
                L'évènement <strong>{{ event.name }}</strong> a été ajouté avec succès.
            </p>
            <hr>

            <h3 class="title is-4">Récapitulatif de l'évènement :</h3>
            <p><strong>Nom :</strong> {{ event.name }}</p>
            <p><strong>Localisation :</strong> {{ event.locationLatitude }} / {{ event.locationLongitude }}</p>
            <p><strong>Date :</strong> {{ event.date|date('d/m/Y') }}</p>

            <div class="mt-4">
                <a href="{{ path('app_home') }}" class="button is-primary">Retour à l'accueil</a>
            </div>
        </div>
    </section>
{% endblock %}
